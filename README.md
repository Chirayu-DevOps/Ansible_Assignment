# Ansible_Assignment
Ansible is an agentless automation tool used for configuration management and deployment. It connects to remote servers via SSH and executes tasks defined in YAML playbooks. In this assignment, Ansible was installed on a control node, connected to a managed node, and used to install and configure Nginx, demonstrating automated and repeatable server configuration.
